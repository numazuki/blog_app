<p><%= @tag.name %>に関連する記事</p>

<% if @articles.exists? %>
  <% @articles.each do|a| %>
    <p><%= link_to a.article.title, article_path(a.article.id) %></p>
  <% end %>
<% else %>
  <p>該当する記事はありません</p>
<% end %>